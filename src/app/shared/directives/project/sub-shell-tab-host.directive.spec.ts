import { SubShellTabHostDirective } from './sub-shell-tab-host.directive';

describe('SubShellTabHostDirective', () => {
  it('should create an instance', () => {
    const directive = new SubShellTabHostDirective();
    expect(directive).toBeTruthy();
  });
});
